Verifyscreen{
auth.obj.
User.obj.
Timer.obj.

initState(){

user=User.currentUser;
user.sendEmailVerification();
Time.periodic(Duration(Seconds:5),(timer){
checkemailverify();
}
}

dispose(){
timer.cancel();
}

Future<void>checkemailverify() async{

user=auth.currentuser;
await user.reload();
if(user.emailverified){
time.cancel;
Navigate to home page}

